apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: agent-deny-all-egress
  namespace: lap-system
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: lap-agent
  policyTypes:
    - Egress
  egress: []
