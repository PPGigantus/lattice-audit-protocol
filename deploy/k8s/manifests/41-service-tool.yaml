apiVersion: v1
kind: Service
metadata:
  name: lap-tool
  namespace: lap-system
  labels:
    app.kubernetes.io/name: lap-tool
spec:
  selector:
    app.kubernetes.io/name: lap-tool
  ports:
    - name: http
      port: 9000
      targetPort: 9000
  type: ClusterIP
